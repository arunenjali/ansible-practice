- name : Installing and starting the Nginx server
  hosts : frontend
  become : yes
  tasks :
  - name : ngins installation
    ansible.builtin.package:
      name : nginx
      state : present 
  
  - name : Starting the Nginx services
    ansible.builtin.service:
      name : nginx
      state : started
      enabled : yes